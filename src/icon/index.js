import nextButton from './nextButton.svg';
import prevButton from './prevButton.svg';
import closeButton from './x.svg';
import place from './location.svg';
import navArrow from './navArrow.svg';
import cleanInput from './cleanInput.svg';

const icons = {
  nextButton,
  prevButton,
  closeButton,
  place,
  navArrow,
  cleanInput,
};

export default icons;
